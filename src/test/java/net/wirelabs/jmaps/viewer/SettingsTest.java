package net.wirelabs.jmaps.viewer;



import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

import java.nio.file.Paths;

/**
 * Created 5/21/23 by Michał Szwaczko (mikey@wirelabs.net)
 */
class SettingsTest {
/*

    @Test
    void testDefaultSettings() {
        String home = System.getProperty("user.home");
        //MapViewerSettings.setCacheRoot(Paths.get(home,"/.jmaps-cache").toString());
        MapViewerConfig.setUserAgent("JMaps Tiler v.1.0");

        assertThat(MapViewerConfig.getUserAgent()).isEqualTo("JMaps Tiler v.1.0");
        assertThat(MapViewerConfig.getTileCacheDir()).isEqualTo(Paths.get(home,".jmaps-cache","tile-cache").toString());
        assertThat(MapViewerConfig.getWmtsCacheDir()).isEqualTo(Paths.get(home,"/.jmaps-cache","wmts-cache").toString());
    }

    @Test
    void testCustomSettings() {
        String temp = System.getProperty("java.io.tmpdir");
        MapViewerConfig.setWmtsCacheDir(Paths.get(temp, "alamakota", "wmts-cache").toString());
        MapViewerConfig.setTileCacheDir(Paths.get(temp, "alamakota", "tile-cache").toString());
        //set custom settings
      //  MapViewerConfig.setCacheRoot(Paths.get(temp,"alamakota").toString());
        MapViewerConfig.setUserAgent("Bulbulator 123");

        assertThat(MapViewerConfig.getUserAgent()).isEqualTo("Bulbulator 123");
        assertThat(MapViewerConfig.getTileCacheDir()).isEqualTo(Paths.get(temp,"alamakota","tile-cache").toString());
        assertThat(MapViewerConfig.getWmtsCacheDir()).isEqualTo(Paths.get(temp,"alamakota","wmts-cache").toString());
    }
*/

}